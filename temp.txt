- Fix weird html warning in console
- Fix relative paths in codebase documentation (seems to be a dev issue, not in production)
- Make also preset tasks dynamic (make them use buildDynamicPrompts or something)
- Make XML command parsing more lenient?
  * it's okay for CDATA not to close with ]]> as long as </file_content></file> is there (are we sure? we probably want something more unique)